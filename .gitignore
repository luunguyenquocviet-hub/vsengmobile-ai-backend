# Bí mật & môi trường — TUYỆT ĐỐI không đưa lên git
.env
*.env

# Python
__pycache__/
*.pyc
.venv/
venv/

# Dữ liệu nặng / model tái sinh được (tùy chọn: bỏ comment nếu không muốn commit)
# *.xlsx
# *.json

# Flutter (nếu commit chung repo)
flutter_app/build/
flutter_app/.dart_tool/

# Hệ điều hành / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/